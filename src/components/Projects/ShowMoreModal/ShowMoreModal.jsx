


export const ShowMoreModal = ({ onClose, item }) => {

    // const { description, features, tools, role } = item;
    return (
        <div>ShowMoreModal
            <button onClick={onClose}></button>
        </div>
    )
}